# 202530127 정원영
## JAVA2
<br>

# 2026.04.08

## 메소드의 배열 리턴
- 배열의 레퍼런스만 리턴
- 배열 전체 리턴 X

### 메소드의 리턴 타입
- 리턴하는 배열 타입과 리턴 받는 배열 타입 일치
- 리턴 타입에 배열의 크기를 지정하지 않음
```java
// 리턴 타입[] / 메소드 이름()
int[] makeArray() {
    int temp[] = new int[4];
    return temp;
    // return 배열 리턴 
}
```


## 2차원 배열의 초기화
```java
int intArrayp[][] = {
    {0,1,2},
    {3,4,5},
    {6,7,8};
}
```

## 2차원 배열
### 2차원 배열 선언
```java
int intArray[][]; 
int[][] intArray2;
```
### 2차원 배열 생성
```java
intArray = new int[2][5];
int intArray[] new int[2][5];
```
### 2차원 배열의 length 필드
```java
i.length // 2차원 배열의 행의 개수로, 2
i[n].length // n 번째 행의 열의 개수 
i[1].length // 1번째 행의 열의 개수, 5 
```
## 배열과 for-each 문
- for - each 문 : 배열이나 나열(enumeration)의 원소를 순차 접근하는데 유용한 for 문
```java
for(변수 : 배열레퍼런스) {
    .. 반복 작업문 ..
}
```
- for - each 문으로 정수 배열의 합을 구하는 사례
```java
// for - each 문 으로 구현
int [] n = {1,2,3,4,5};
int sum = 0;
for (int k : n) {
    sum += k;
    // 반복 할 때 마다 n의 0 번째 부터 끝까지 k 의 값이 바뀐다.
    // k =1 (n[0]), k = 2(n[1]) ...
}
```
```java
// 위에 코드를 for 문으로 구현
for (int i = 0; i < n.length;i++) {
    int k = n[i];
    sum += k;
}
```
## 배열의 크기, length 필드
- 자바의 배열은 객체로 처리
- 배열의 크기는 배열 객체의 length 필드에 저장
```java
int intArray[];
intArray[] = new int[5];

int size = intArray.length;
// size는 5 이다.
```
- length 필드를 이용하여 배열의 모든 값을 출력하는 사례
```java
for (int i =0 ;i<intArray.length;i++) System.out.println(intArray[i]);
```


# 2026.04.01
## 레퍼런스 치환과 배열 공유
- 레퍼런스 치환으로 두 레퍼런스가 하나의 배열 공유
```java
int intArray[] = new int[5];
int myArray[] = intArray;
// 같은 메모리 주소
intArray[] = 2;
myArray[]=6;
//둘다 6 이됨
```

## 배열 인덱스와 배열 원소 접근
- 배열의 인덱스는 0부터, 크기 1부터
```java
int intArray = new int[5];
intArray[0] = 5;
intArray[3] = 6;
int n = intArray[3]; 
```
- 인덱스를 잘못 사용한 경우
```java
int n = intArray[-2] // errer 인덱스로 음수 사용 불가
int m = intArray[5]; // errer 5는 인덱스 범위 나감
```
- 반드시 배열 생성 후 접근
```java
int intArray []; // 레퍼런스만 선언
intArray[1] = 8; // errer. 배열이 생성되어 있지 않음

// 옳은 접근
int intArrayp[] = new int[5];
```


## 자바 배열 (array)
- `인덱스`와 인덱스에 `대응하는 데이터`들로 이루어진 자료 구조로 `한 번에 많은 메모리 공간선언`
- `같은 타입의 데이터`들이 순차적으로 저장되는 공간으로 `인덱스를 이용하여 원소 데이터 접근`
- `반복문을 이용하여 처리`하기에 적합한 자료 구조
- 배열 인덱스 : 0 부터 시작
```java
// 배열에 대한 레퍼런스 변수 intArray 선언
int intArray [];
/*
int = [배열 타임]
intArray = [배열에 대한 레퍼런스 변수]
[] = [배열 선언]
*/ 


// 배열 생성
int Array = new int [5];
[배열에 대한 레퍼런스 변수] [배열 생성] [타입] [원소 개수]
/*
int = [배열에 대한 레퍼런스 변수] 
Array = [배열 생성]
int =  [타입]
[5] =  [원소 개수]

*/
```
### 배열 선언 및 생성 디테일
- 배열은 선언과 생성의 두 단계 필요 : 선언과 동시에 생성할 수 있음
- 배열 선언: 배열의 이름 선언 (배열 레퍼런스 변수 선언)
```java
int intArray []; 
int[] intArray;
```
- 배열 생성 : 배열 공간 할당 받는 과정
```java
intArray = new int[5];
```
- 배열 초기화 : 배열 생성과 값 초기화
```java
int intArray[] = {4,3,2,1,0};
double doubleArray[] = {0.01,0.02,0.03,0.04}
```

## break 문
- 반복문 하나를 즉시 벗어날 때 사용. 하나의 반복문만 벗어남
- 중첨 반복의 경우 안쪽 반복문의 break문이 실행되면 반복문만 벗어남
``` java
for(초기문;조건식; 반복 후 작업) {
    .....................
    break;
    .....................
}
```
```java
for(초기문;조건식; 반복 후 작업) {
   while(조건식) {
        .....................
        break;
        .....................
   }
   .....................
}
.....................

```
## contunue 문
- 반복문을 빠져 나가지 않고, 다음 반복으로 제어 변경.
- 반복문에서 contunue; 문에 의한 분기

## 중첩 반복
- 반복문이 다른 반복문을 내포하는 구조
```java
for(int i =0;i<10;i++) {
    for (int j = 0;j<10;j++)
}
```
## 반복문 - do_while
- w조건식이 '참'인 동안 반복 실행. `작업문 한 번 반드시 실행 `
```java
애 {
    ... 작업문 ...
}while(조건식)
```
## 반복문 - while
- 조건식이 '참'인 동안 반복 실행
```java
while(조건식) {
    ... 작업문 ...
}
```

## 반복문 - for
- 자바 반복문 - for 문, while 문, do-while문
``` java
for(초기문;조건식:반복후 작업) {
    ... 작업 문 ...
}
```

## swtich문
- Switch문의 식과 case문의 값과 비교
1. case의 비교 값과 일치하면 해당 case 실행문장 수행
2. break를 만나면 switch문을 벗어남
3. case의 비교 값과 일치하는 것이 없으면 default 문 실행
4. default 생략 가능
```java
switch(변수) {
    case 조건1 :
        ... 실행문장 ...
    case 조건2 :
        ... 실행문장 ...
    case 조건3 :
        ... 실행문장 ...
}
 
```
### case 문의 값
문자, 정수, 문자열 리터럴(JDK 1.7) 만 허용
<br>
실수 리터럴은 허용되지 않음
## 다중 if-else 문
- 다중 if 문
- 조건문이 너무 많은 경우, switch 문 사용ㄱ ㅝㄴ장

```java
if(조건식1) {
... 실행 문장 ...
}
else if (조건식2) {
    ... 실행 문장 ...
}
else if (조건식3) {
    ... 실행 문장 ...
}
```

## 조건문 - 단순 if 문, if - else 문
- 단순 if 문
    - if의 괄호 안에서 조건식(논리형 변수나 논리 연산)
    - 실행문장이 단일 문장일 경우 둘러싸는 {, } 생략 가능
```java
if(조건식) {
    ... 실행 문장 ... // 조건식이 참인 경우
}
```
- if - else 문
``` java
if(조건식) {
    ... 실행 문장 ... // 조건식이 참인 경우
}
else {
    ... 실행 문장 ... //if 조건에 안 맞을 경우
}
```

## 비트 연산
- 비트 개념 
```java
byte x = 10;
x0001010
// 숫자 1개가 비트
```
- 비트 논리 연산 : 비트끼리 AND, OR,XOR, NOT 연산
- 비트 시프트 연산 : 비트를 오른쪽이나 왼쪽으로 이동

## 비트 연산이 사용되는 경우
- 비트 연산(Bitwise Operations)은 하드웨어 프로그래밍 뿐만 아니라 일반 `소프트웨어 개발에서도 여러 가지 용도로 사용`
- 특히 `성능이 중요한 경우`, `최적화가 필요한 경우`에 많이 활용
<br>
 1. 성능 최적화 및 연산 속도 향상 : 곱셉(*)과 나눗셈(/)보다 비트 연산(<<,>>)이 훨씬 빠름
```java
int x;
int result = x <<1 // 5 * 2 = 10
System.out.println(result); // 10
```
2. 권한 및 플래그 설정 (비트 마스크) : 여러 개의 상태(flag)를 하나의 int 변수에 저장할 때 사용
3. 데이터 압축 및 최적화
    - 불필요한 공간을 줄이기 위해 여러 개의 작은 값을 하나의 정수에 저장
4. 해싱 및 암호와
5. 덧셈 연산

## 삼항 연산자
- 3 개의 피연산자로 구성된 삼항(ternary) 연산자로
- `opr1 ? opr2 : opr3` -> opr1이 결과가, true면 opr2, false면 opr3 
- if-else 을 조건연산자로 간결하게 표현 가능

# 2026.03.25
## 비교 연산, 논리 연산
- 비교 연산자 : 두 개의 값을 비교하여 true/ false 결과
- 논리연산자 : 두 개의 논리 값에 논리 연산. 논리 결과
```java
// 비교 연산자
a < b
a > b
a <= b
a >= b
a == b
a != b
```
```java
// 논리 연산자
!a
a ^ b
a || b
a && b
```
## 증감 연산
- 1 증가 혹은 감소 시키는 연산 : `++`, `--`
### 전위 연산자
```java
a = 1;
b = ++a;
// 결과
// a = 2
// b = 2
```
### 후위 연산자
```java
a = 1;
b = a++;
// 결과
// a = 2
// b = 1
```
## 연산자 
연산 : 주어진 식을 계산하여 결과를 얻어내느 과정
- 증감 : `++ , --`
- 산술 : `+ - * / %`
### 산술 연산자
산술 연산자 : + - / * %

## 한글깨짐

## System.in
- 키보드와 연결된 자바의 표준 입력 스트림
- 입력되는 키를 바이트로 리턴하는저수준 
- system.in을 직접 사용하면 바이트를 문자나 숫자로 변환하는 많은 어려움 있음
## Scanner 클래스
- 읽은 바이트를 문자, 정수, 실수, 불린, 문자열 등 다양한 타입으로 변환하여 리턴.
## java.util.Scanner
- 객체를 생성해서 사용
- 키보드에 연결된 System.in에게 키를 읽게 하고, 원하는 타입으로 변환하여 리턴
- 입력되는 키 값을 `공백으로 구분`되는 `토큰 단위`로 읽음
- 공백 문자 : `\t`,`\r`,`\n`,` `,`\f` (페이지 나누기, 폼 피드, 프린트에서 사용)
## 타입 변환
- 특정 데이터 타입의 값ㅇ르 `다른 데이터 타입의 값으로 변환.`
### 자동 타입 변환
- 컴파일러에 의해 원래의 타입보다 큰 타입으로 자동 변환
- 치환문(=)이나 수식 내에서 `타입이 일치 하지 않을 때`
```java
long m = 25; // 25는 int 타입 25가 long 타입으로 자동변환
double d = 3.14 * 10 // 실수 연산 위해 10이 10.0으로 자동 변환
```
### 강제 타입변환
- 개발자의 의도적  타입 변환
- ( ) 안에 개발자가 명시적으로 타입 변환 지정
- 강제 변환은 값 손실 우려
큰 데이터 타입에서 작은 데이터 타입으로는 변환 되지 않습니다.
<br>
int -> byte 등은 오류가 발생합니다
## System.out.printlf() 의 종류

## var 키워드
- java 10 부터 도입
- var 키워드는 타입을 생략하고 변수 선언
- 컴파일러가 추론하여 변수 타입을 결정
- 변수 선언할 때 초기값이 주지 않으면 컴파일 오류가 발생
- var는 `지역 변수 선언에만 사용`이 가능하고 `클래스 필드에서는 사용할 수 없습니다.`
    - 지역 변수  : 메소드 내부에 선언되는 변수
    - 클래스 필드 : 클래스 내부에 선언되는 변수. 객체가 생성될 때 함께 만들어지는 변수
- 다음과 같이 사용하는 것이 좋다!
    - 기본적으로 명시적 자료형(int, String, double 등)을 사용하는 것이 좋다
    - 가독성이 유지될 수 있는 경우에 한해서 var를 적절히 활용하는 것이 좋습니다.
    - 특히, 상수를 적극적으로 활용해서 코드의 안정성을 높이는 것이 중요합니다.
## 상수의 선언
final 로 선언

## 변수의 선언
- 변수 : 프로그램 실행 중에 값을 임시로 저장 하기 위한 메모리의 공간으로 프로그램 수행 중 변경될 수 있음.
- 변수 선언

## 메모리의 구조
- 힙(heap -FIFO) 영역은 프로그래머가 직접 공간을 할당, 해제하는 메모리 공간 입니다. Java의 경우 JVM이 담당
- 스택(stack - LIFO) 영역은 `프로그램이 자동으로 사용하는 임시 메모리 영역`
- 힙이 스택을 침범하는 경우를 힙 오버 플로우라 하고 스택이 힙을 침범하는 경우를 스택 오버 플로우 라고 합니다.

## Java의 데이터 타입
- 기본 자료형 8개 : boolean,char,byte,shrot,int,long,float,double
- 기본 타입의 크기는 cpu나 운영체제에 따라 변하지 않음
- 레퍼런스형 1개이며 용도는 다음 3가지: 포인터와 유사한 개념이지만 메모리 주소 X
### 문자열
문자열은 기본 타입 X
String 클래스로 문자열 표현
- 문자열 리터럴(literal) : `"JDK","한글"`
### 참조 자료형
- 포인터는 임의의 메모리 주소를 저장, 참조 자료형은 주소를 저장할 수 없습니다.
- 직접 주소를 갖고 있지는 않지만, JVM이 해당 주소로 안내해 주게 된다.
- `객체를 참조하는 변수 유형`, Heap 영역에 저장된 객체의 메모리 주소를 가리킴
- New 키워드로 `객체(인스턴스)를 생성`하는 것이 바로 `참조 자료형을 선언하는 것`, 이렇게 선언된 참조 자료형은 JVM이 대신 객체의 주소를 저장
- 배열, 인터페이스 혹은 열거형도 객체 이기 때문에 참조 자료형
- 객체를 참조하지 않을 때 null 값을 가질수 있다.
- `같은 객체를 여러 변수가 참조`할 수 있고, `=연산자로 객체의 주소 비교`할 수도 있습니다.
## Temurin OpenJDK 24 에서 jmods 디렉토리가 사라진 이유
- JEP (Java Enhancement Proposal) : Java 기능 제안서
- java 플랫폼의 새로운 기능, 성능 향상, 혹은 언어적 변화를 제안하고 상세히 기술하는 표준 문서
- Adoptium은 Eclipse Foundation이 관리하는 오픈소스 프로젝트
## 소스코드, 바이트 코드, 기계어
### 소스코드
- 우리가 작성하는 java 코드
- 사람이 읽을 수 있는 고수준 언어
### 바이트코드 
- java 컴파일러가 소스코드를 변환한 중간 코드
- cpu가 직접 실행 x -> JVM 이 실행해야 함
- 기계어와 다르게 플랫폼 독립적
### 기계어 
- cpu가 직접 실행할 수 있는 0과 1의 이진 코드
- 운영체제(os)와 cpu 아키텍쳐에 따라 다름
- 15진수 형태의 기계어

### 바이트코드와 기계어 차이점?
- 바이트코드는 jvm이 실행하는 중간 코드 -> 운영체제와 cpu에 관계없이 사용 가능
- 기계어는 cpu가 직접 실행하는 코드 -> 특정 하드웨어에 종속됨
## java 특징 (3)
- 실시간 응용프로그램에 부적함
    - 실행 도중 예측할 수 없는 시점에 가비지 컬렉션 실행 때문
    - 응용 프로그램의 `일시적 중단 발생`
- 자바 프로그램은 안전
    - `타입 체크 엄격`
    - 물리적 주소 ??? 를 사용하는 포인터 개념 없음
- 프로그램 작성 쉬움    
    - 포인터 개념 X
    - 동적 메모리 반환 X
    - 다양한 라이브러리 지원
- 실행 속도 개선을 위한 JIT 컴파일러 사용
    - 바이트 코드를 인터프리터 방식으로 실행
    - 기계어가 실행되는 것보다 느림
    - JIT 컴파일 기법으로 실행 속도 개선
    - JIT 컴파일 : 실행 중에 바이트 코드를 기계어 코드로 컴파일하여 기계어를 실행하는 기법

# 2026.03.18
## 자바의 특징
### 실행 코드 배포
- 한개의 class 파일 또는 다수의 class 파일로 구성

### 패키지
- 서로 관련 있는 여러 클래스를 패키지로 묶어 관리
- 패키지는 폴더 개념

### 멀티스레드
- 여러 스레드의 동시 수행 환경 지원
- 자바는 `운영체제의 도움없이 자체적으로 멀티스레드 지원
- c/c++ 프로그램은 멀티스레드를 위해 운영체제 
### 가비지 컬렉션
- 자바 언어는 `메로리 할당 기능은 있어도 메모리 반환 기능 없음`
- 사용하지 않는 메모리는 JVM에 의해 자동 반환 : 가비지 컬렉션
--- 
## Java API
- 자바 api 
## 모듈화
모듈화 : java 9에서 정의된 새로운 기능
<br>
모듈 : 자바 패키지들과 이미지, XML 파일 등의 자원들을 묶는 다위
<br>
모듈 프로그래밍 : 프로그램을 레고를 만들 듯이 필요한 모듈을 연결하는 방식으로 작성
<br>

### java 플랫폼의 모듈화
- 실행시간에 사용되는 자바 API의 모든 클래스들을 모듈로 분활
- 세말한 모듈화, 자바 응용프로그램이 실행되는데 필요 없는 모듈 대체
### 모듈 리스트(java se)
- java9 부터 SE의 모든 클래스들을 모듈로 재구성
- JDK의 설치 디렉터리 밑의 jmods 디렉토리서 확인할 수 있음
## java 표준안
- java SE(Standard Edition)
    - 테스크톱, 서버, 임베디드 시스템을 위한 표준 자바 플랫폼

- Java EE (Enterprise Edition)
    - 자바를 이용한 `서버측 개발을 위한 플랫폼`
- Java ME(Micro Edition)
    - 임베디드를 위한 자바 플랫폼
## JDK 와 JRE
jdk의 bin 디렉터리에 포함된 주요 개발 도구
- javac  
- java
- javadoc
- jar
- ilink
- idb
- iavap
## Java JVM과 자바 실행 환경
- 실행 환경은 `JVM + JAVA API (클래스 라이브러리)로 구성.
- 응용프로그램의 실행
    - main() 메소드를 가진 클래스의 main()에서 실행 시작.
    - JVM은 필요할 때 클래스 파일 로딩하기 떄문에 `적은 메모리로도 실행 가능`
### 바이트 코드 (.class)
- 자바 JVM에서 실행 가능한 `바이너리 코드`
- 바이트 코드는 컴퓨터 `CPU에 의해 직접 실행되지 않음`
- 자바 `JVM이 작동 중인 플랫폼에서 실행`
- 자바 JVM이 `인터프리터 방식`으로 바이트 코드 해석
### JVM(Java Virtual Machine)
- `각기 다른 플랫폼에 맞는 JVM을 제공`. `JVM 자체는 플랫폼에 종속적`.
- JVM 각기 다른 플랫폼에서도 동일한 자바 실행 환경을 제공
- JVM은 자바 개발사인 오라클 외 IBM, MS 등 다양한 회사에서 제작 공급.
- 자바의 실행은 JVM에서 클래스 파일(.class)을 바이트 코드 실행 하는 것
## WORA(Write Once Run Anywhere)
- 한번 작성된 코드는 OS, H/W에 상관없이 ` 모든 플랫폼에서 바로 실행` 되는 자바의 특징
- C/C++ 등 기존 언어가 가진 플랫폼 종속성 극복
- 네트워크에 연결된 어느 클라이언트에서나 실행, 웹 브라우저. 분산 환경 지원.
## WORA를 가능하게 하는 자바의 특징
- 바이트 코드(byte code)
    - 자바 소스를 `컴파일한 목적 코드`
    - CPU에 종속적이지 않은 `중립적인 코드`
    - `JVM에 의해 해석되고 실행
- JVM(Java Virtual Machine)
    - 자바 바이트 코드를 실행하는 자바 JVM(소프트웨어)
## 플랫폼
플랫폼 = 하드웨어 플랫폼 + 운영체제 플랫폼
<br>
프로그램의 플랫폼 `호환성 없는 이유 `
- 기겨어가 CPU마다 다름
- 운영체제마다 API 다름
- 운영체제마다 실행차일 형식 다름
## 자바를 만든 목적
- 플랫폼 호환성 문제 해결
    - `기존 언어`로 작성된 프로그램은 `플랫폼 간에 호환성 없음`
- 플랫폼 독립적인 언어 개발
    - `모든 플랫폼에서 호환성을 갖는 프로그래밍 언어 필요`
    - 네트워크, 특히 `웹에 최적화된 프로그래밍 언어의 필요성 대두`
- 메모리 사용량이 적고 다양한 플랫폼을 가지는 가전 제품에 적용
    - 가전제품 : 작은 량의 메모리를 가지는 제어 장치
    - 내장형 세스템 오구 총족

## 프로그래밍과 컴파일
소스 : 프로그래밍 언어로 작성된 `텍스트 파일`
<br>
컴파일 : 소스 파일을 컴퓨터가 이해할 수 있는 `기계어로 만드는 과정`
<br>
자바 : .java -> .class
<br>
C   : .c -> .obj -> .exe
<br>
C++ : .cpp -> .obj -> .exe


## 프로그램 언어의 패러다임 변화
### 절차 지향 언어
- 프로그램을 `절차, 순서에 따라서 실행하는 방식`
- 데이터(입력)와 함수를 분리 하여 작성
- 코드의 `유현성 부족`, `재사용이 어렵다`.
- `전역 변수를 많이 사용`하기 때문에 코드의 `가독성과 유지보수가 어려워`진다.
- C,Pascal, Fortran 등
### 객체 지향 언어
- `현실의 객체를 모델링`하여 프로그램을 작성
- 객체는 `데이터와 데이터를 처리하는 메소드(함수)를 모두 포함`
- 상속, 캡슐화, 다형성 등의 개념을 활용하여 `유연하고 재사용 가능한 코드` 를 작성 가능
- Java,C++,Python
### 함수 지향 언어
- 함수형 언어는 `함수를 일급 객체로 취급`,`상태 변경을 피하고 불변성을 지향하는 프로그래밍 패러다임`
- 함수의 조합으로 복잡한 작업을 수행, `상태 변경 대신에 데이터를 변환 하는 방식`
- `재귀, 고차 함수, 순수 함수 등의 개념을 중요시함`
- `병렬 처리와 높은 수준의 추상화 지원`
- 함수 지향 = 객체 지향 언어의 특성 그대로 + 함수 지향 적인 기능 추가
- Python, Kotlin, Haskell, Lisp, Scala
---
## 프로그래밍 언어
### 기계어
- 0,1의 이진수로 구성된 언어
- 컴퓨터의 cpu는 기계어만 이해하고 처리 가능
### 어셈블리어
- 기계어 명령을 ADD, SUB, MOVE 등과 같은 표현하기 쉬운 사징적인 단어인 니모닉 기호(mnemonic symbol)로 일대일 대응시킨 언어
### 고급언어
- 사람이 사용하는 언어
- 이해하기 쉽다.
- 쉽게 표현 할수 있도록 고안 됨
- 예시
    - Pascal
    - Basic
    - C/C++
    - Java
    - C#
    - 절차지향, 객체지향으로 나눌수 있다.


## 변수 선언법
낙타 표기법
## vscode에서 java 프로젝트 생성법
```
ctrl + shift + p
```
누른 후 
1. >java:Create java project 
2. No build tools 
선택해서 프로젝트를 만든다

# 2026.03.11

### 기본 Branch가 Master 에서 Main 으로 바뀐 이유
과거 Black Lives Matter 운동을 계기로 주종 관계를 뜻하는 master, slave 라는 용어를 다른 단어로 대체하기 위해서

```java
public class Main {
    public static void main(Stringp[] args) {
        System.out.println("Hello world");
    }
}
```

git push , 기초 설정
```bash
git config user.name foo
git config user.email foo@exam.com
```